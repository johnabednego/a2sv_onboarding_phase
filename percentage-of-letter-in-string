class Solution:
    def percentageLetter(self, s: str, letter: str) -> int:
        counter = 0
        for i in s:
            if i==letter:
                counter+=1
        if counter != 0:
            return math.floor(counter/len(s)*100)
        else:
            return 0
