class Solution:
    def targetIndices(self, nums: List[int], target: int) -> List[int]:
        nums.sort()
        index_array = []
        for i in range(len(nums)):
            if nums[i]==target:
                index_array.append(i)
        return index_array
