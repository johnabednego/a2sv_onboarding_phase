class Solution:
    def isPalindrome(self, x: int) -> bool:
        l =0
        r=-1
        while l<len(str(x)):
            left = str(x)[l]
            right = str(x)[r]
            if left!=right:
                return False
            l+=1;
            r-=1
        return True
